# Issue: https://github.com/znas-io/asyncapi-codegen/issues/101

# Context modification were not passed between middlewares

asyncapi: 2.6.0
info:
  title: Test101 application
  version: '1.0.0'
channels:
  test101:
    publish:
      message:
        payload:
          type: string
