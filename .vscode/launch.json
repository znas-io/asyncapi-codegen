{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/asyncapi-codegen",
            "args": [
                "-i", "${workspaceFolder}/schema/async-api-sample.yaml",
                "-p", "ws",
                "-o", "${workspaceFolder}/codegen/svc-ccs-rtc-bridge.gen.go"
            ]
        }
    ]
}
